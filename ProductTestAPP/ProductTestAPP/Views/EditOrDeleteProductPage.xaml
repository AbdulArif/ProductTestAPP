<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewModels="clr-namespace:ProductTestAPP.ViewModels"
             mc:Ignorable="d"
             x:Class="ProductTestAPP.Views.EditOrDeleteProductPage">
    <!--<ContentPage.BindingContext>
        <viewModels:EditProductViewModel/>
    </ContentPage.BindingContext>-->

    <StackLayout>
        <Entry Text="{Binding Product.Name}" />
        <Entry Text="{Binding Product.Category}" />
        <Entry Text="{Binding Product.Price}" />

        <Button Command="{Binding PutCommand}"
                Text="Edit Product" />

        <Button Command="{Binding DeleteCommand}"
                Text="Delete Product" />

    </StackLayout>
</ContentPage>